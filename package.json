{
  "name": "model-eval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Benchmark harness comparing local models against claude-haiku-4.5 for the Hugo SMS agent.",
  "scripts": {
    "test": "vitest run",
    "bench": "tsx src/run.ts",
    "eval": "tsx src/cli.ts"
  },
  "dependencies": {
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
